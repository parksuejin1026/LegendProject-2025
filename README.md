# ♟️ LEGEND GOMOKU (오목)

## 💡 프로젝트 개요

이 프로젝트는 **PVE (플레이어 대 AI)**, **PVP (로컬 2인용)**, 그리고 **Online (실시간 멀티플레이어)** 모드를 지원하는 웹 기반 오목 게임입니다.
Next.js와 TypeScript로 개발되었으며, Socket.io를 이용한 실시간 대전, MongoDB를 이용한 전적/랭킹 시스템 등 강력한 기능을 제공합니다.

### ✨ 주요 기능

- **🤖 스마트 AI (Minimax)**:
  - Minimax 알고리즘과 Alpha-Beta 가지치기를 적용한 강력한 AI
  - **3단계 난이도**: Easy (초보), Medium (중수), Hard (고수)
  - **AI 생각 시각화**: AI가 두려는 위치의 가중치를 히트맵으로 표시 (힌트 기능)
- **🌍 온라인 멀티플레이어 (Socket.io)**:
  - 실시간으로 방을 만들고 친구를 초대하여 대결 가능
  - 즉각적인 착수 동기화 및 턴 관리
- **🏆 랭킹 및 전적 시스템**:
  - 회원가입/로그인 기능을 통해 개인 전적 저장 (MongoDB)
  - PvE 및 PvP 모드별 명예의 전당 (Top 10 랭킹) 제공
- **🎨 몰입감 있는 UI/UX**:
  - **다양한 테마**: Modern, Wood, Dark, Neon 등 취향에 맞는 테마 선택
  - **고급 애니메이션**: 부드러운 착수 효과, 파티클 배경, 승리 팡파레
  - **사운드 효과**: 돌을 놓을 때의 타격감 있는 사운드 (음소거 가능)
- **📱 모바일 최적화**: 반응형 디자인으로 모바일에서도 쾌적한 플레이

---

## ⚙️ 기술 스택

| 구분 | 기술 스택 | 설명 |
| :--- | :--- | :--- |
| **프레임워크** | **Next.js 14** | React 기반의 풀스택 프레임워크 |
| **언어** | **TypeScript** | 정적 타입을 통한 안정적인 코드 작성 |
| **스타일링** | **Styled-components** | CSS-in-JS를 이용한 컴포넌트 스타일링 |
| **실시간 통신** | **Socket.io** | 웹소켓 기반의 실시간 양방향 통신 |
| **데이터베이스** | **MongoDB (Mongoose)** | 사용자 정보 및 전적 데이터 저장 |
| **상태 관리** | **React Hooks** | `useState`, `useCallback`, `useEffect` 활용 |

---

## 🚀 시작하는 방법

이 프로젝트는 Node.js와 npm을 사용하여 실행됩니다.

1.  **프로젝트 클론 및 이동:**

    ```bash
    git clone https://github.com/parksuejin1026/LegendProject-2025.git
    cd LegendProject-2025
    ```

2.  **환경 변수 설정:**
    프로젝트 루트에 `.env.local` 파일을 생성하고 다음 내용을 추가하세요.
    ```env
    MONGODB_URI=your_mongodb_connection_string
    ```

3.  **의존성 설치:**

    ```bash
    npm install
    ```

4.  **개발 서버 실행:**
    ```bash
    npm run dev
    ```
    브라우저에서 `http://localhost:3000`으로 접속하여 게임을 즐기세요!

---

## 🌐 배포 (Deployment)

이 프로젝트는 Socket.io를 사용하므로 **지속형 서버(Persistent Server)** 환경이 필요합니다.
**Render** 또는 **Railway** 배포를 권장합니다.

1.  **Render** (무료): Web Service 생성 -> Build Command: `npm install && npm run build` -> Start Command: `npm start`
2.  **환경 변수**: 배포 설정 시 `MONGODB_URI`를 반드시 입력해야 합니다.

---

## 📁 프로젝트 구조

```
LegendProject-2025/
├── src/
│   ├── core/           # 게임 핵심 로직 (GomokuGame, AI, Sound, Storage)
│   ├── components/     # UI 컴포넌트 (Board, Lobby, RankingBoard 등)
│   ├── hooks/          # 커스텀 훅 (useGomokuGame, useSocket)
│   ├── models/         # MongoDB 스키마 (User)
│   ├── lib/            # DB 연결 유틸리티
│   └── App.tsx         # 메인 앱 컴포넌트
├── pages/
│   ├── api/            # 백엔드 API (auth, game, socket)
│   └── ...
├── public/             # 정적 파일
└── ...설정 파일들
```
